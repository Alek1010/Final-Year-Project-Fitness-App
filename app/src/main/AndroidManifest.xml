<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.W1965221_FinalYearProject"
        tools:targetApi="31">

        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".auth.SignUpActivity" />

        <activity android:name=".coach.CoachDashboardActivity" />
        <activity android:name=".coach.ClientListActivity"/>
        <activity android:name=".coach.ClientDetailActivity"/>
        <activity android:name=".coach.AdjustProgramActivity"/>


        <activity android:name=".client.ClientDashboardActivity" />
        <activity android:name=".client.TrainingPlanActivity" />
        <activity android:name=".client.MacrosActivity"/>
        <activity android:name=".client.WeightProgressActivity"/>



        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.W1965221_FinalYearProject">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>